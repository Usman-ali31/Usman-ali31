name: Generate GitHub Stats

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate Stats Images
        uses: jstrieb/github-stats@master
        with:
          username: Usman-ali31
          access_token: ${{ secrets.ACCESS_TOKEN }}
